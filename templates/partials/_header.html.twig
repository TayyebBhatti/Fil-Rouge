<header class="site-header">
    <div class="site-header__inner">
        <a class="site-logo" href="{{ path('default_home') }}">EventFlow</a>
        <ul class="site-nav">
            <li><a href="{{ path('default_home') }}">Accueil</a></li>

            {% if app.user %}
                <li><a href="{{ path('user_profil') }}">Mon Profil</a></li>
                <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                <li>
                    <span class="nav-pill">
                        {{ app.user.prenom ?: app.user.nom ?: app.user.email }}
                    </span>
                </li>
            {% else %}
                <li><a href="{{ path('default_login') }}">Connexion</a></li>
                <li><a href="{{ path('default_inscription') }}">Inscription</a></li>
            {% endif %}
        </ul>
    </div>
</header>
