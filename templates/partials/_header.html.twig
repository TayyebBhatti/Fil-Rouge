<nav class="navbar">
    <div class="logo">EventFlow</div>
    <ul class="nav-links">
        <li><a href="{{ path('default_home') }}">Accueil</a></li>

        {% if app.user %}
            <li>
                <a href="{{ path('user_profil') }}">Mon Profil</a>
            </li>
            <li>
                <a href="{{ path('app_logout') }}">Déconnexion</a>
            </li>
            <li>
                <span style="padding:4px 10px;background:#0ea5e9;color:white;border-radius:6px;font-size:0.9rem;">
                    {{ app.user.prenom ?: app.user.nom ?: app.user.email }}
                </span>
            </li>
        {% else %}
            <li>
                <a href="{{ path('default_login') }}">Connexion</a>
            </li>
            <li>
                <a href="{{ path('default_inscription') }}">Inscription</a>
            </li>
        {% endif %}
    </ul>
</nav>

<style>
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    background: #222;
    color: #fff;
}
.logo {
    font-size: 1.3rem;
    font-weight: bold;
}
.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    align-items: center;
}
.nav-links a {
    color: #fff;
    text-decoration: none;
    padding: 8px 12px;
    border-radius: 6px;
    transition: background 0.3s;
}
.nav-links a:hover {
    background: rgba(255, 255, 255, 0.1);
}
.nav-links span {
    display: inline-block;
}
</style>